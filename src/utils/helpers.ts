import { clsx, type ClassValue } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs: ClassValue[]) {\n  return twMerge(clsx(inputs));\n}\n\nexport function formatCurrency(amount: number, currency = 'USD'): string {\n  return new Intl.NumberFormat('en-US', {\n    style: 'currency',\n    currency,\n    minimumFractionDigits: 2,\n    maximumFractionDigits: 2,\n  }).format(amount / 100);\n}\n\nexport function formatDate(date: Date | string): string {\n  return new Intl.DateTimeFormat('en-US', {\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric',\n  }).format(new Date(date));\n}\n\nexport function formatDateTime(date: Date | string): string {\n  return new Intl.DateTimeFormat('en-US', {\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric',\n    hour: '2-digit',\n    minute: '2-digit',\n    second: '2-digit',\n  }).format(new Date(date));\n}\n\nexport function getStatusColor(status: string): string {\n  const statusMap: Record<string, string> = {\n    pending: 'bg-warning bg-opacity-10 text-warning',\n    approved: 'bg-success bg-opacity-10 text-success',\n    rejected: 'bg-danger bg-opacity-10 text-danger',\n    active: 'bg-success bg-opacity-10 text-success',\n    completed: 'bg-info bg-opacity-10 text-info',\n    cancelled: 'bg-danger bg-opacity-10 text-danger',\n    open: 'bg-info bg-opacity-10 text-info',\n    'in-progress': 'bg-warning bg-opacity-10 text-warning',\n    resolved: 'bg-success bg-opacity-10 text-success',\n    closed: 'bg-secondary-100 text-secondary-700',\n  };\n  return statusMap[status.toLowerCase()] || 'bg-secondary-100 text-secondary-700';\n}\n\nexport function getInitials(firstName?: string, lastName?: string): string {\n  return `${firstName?.charAt(0) || ''}${lastName?.charAt(0) || ''}`.toUpperCase() || 'U';\n}\n\nexport function validateEmail(email: string): boolean {\n  const regex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n  return regex.test(email);\n}\n\nexport function validatePassword(password: string): boolean {\n  return password.length >= 8;\n}\n\nexport async function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\nexport function calculateROI(principal: number, roiPercentage: number, duration: number, type: string): number {\n  const dailyRate = roiPercentage / 100 / 365;\n  const daysInPeriod = type === 'DAILY' ? 1 : type === 'WEEKLY' ? 7 : type === 'MONTHLY' ? 30 : duration;\n  return principal * dailyRate * daysInPeriod;\n}\n